#include <stdio.h>
#define LINHAS (4)
#define COLUNAS (4)

void populaMatriz(int linhas, int colunas, int m[linhas][colunas){
  for (int i = 0, int < linhas, i++){
    for (int j = 0, j < colunas, j++){
    printf("M[%d][%d]: ", i, j);
    scanf("%d", &m[i][j]);
    }
  }
    }
